============
Installation
============

At the command line::

    $ easy_install django-mantis-authoring

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mantis-authoring
    $ pip install django-mantis-authoring